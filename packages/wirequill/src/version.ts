/**
 * Single source of truth for the version reported by the CLI and written into
 * session rows. Kept in sync with `package.json` by `test/unit/version.test.ts`.
 */
export const WIREQUILL_VERSION = '0.1.1';
